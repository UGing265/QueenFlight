# SETUP INSTRUCTIONS
# 1. Create a file named ".env.local" in this directory (frontend).
# 2. Paste the following content into it:

NEXT_PUBLIC_MAPBOX_TOKEN=PASTE_YOUR_TOKEN_HERE
# Updated Port: Backend runs on 5026 (HTTP) by default
NEXT_PUBLIC_API_URL=http://localhost:5026
